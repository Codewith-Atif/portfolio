# Excel deliverable

Run `python python/create_excel.py` after installing requirements. It creates `excel/Airport_Analytics.xlsx` with:

- formatted flight-level data table
- KPI summary sheet
- airline performance sheet
- terminal performance sheet
- frozen panes, filters and percentage/decimal formats

The workbook can also be used as the source for Power BI or Tableau.
